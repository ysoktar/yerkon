"""YERKON comparison-table simulation.

This package exists to produce four rows of one comparison table: the
urban, rural and critical-zone deployment cases described in the YERKON
presentation, simulated rather than asserted, with the provenance of every
input attached to every output.

Entry point: ``python run.py`` at the repository root.
"""

__version__ = "1.0.0"
