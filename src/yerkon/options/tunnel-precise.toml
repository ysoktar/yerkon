# Bir YERKON yerleşim seçeneği. / A YERKON deployment option.
#
# `--option tunnel-precise` ile defaults.toml üzerine uygulanır; yalnızca
# farklı olan değerler listelenir.
#
# Applied over defaults.toml with `--option tunnel-precise`. Only the
# figures that differ are listed; everything else stays as it is.

name = 'tunnel-precise'
title = 'Tünel: 120 m aralık, kullanılabilirlik ≥ %99,0 ve HPE P50 ≤ 1,00 m için'
title_en = 'Tunnel: 120 m spacing, for availability ≥ 99,0 % and HPE P50 ≤ 1,00 m'
origin = 'yerkon solve'
note = """Tünel askıları 250 m yerine 120 m'de. Bir yerleşim araması, tünel satırının düzenlerini üzerinde durduğu gerçek zemine karşı denedi ve kullanılabilirlik ≥ %99,0, HPE P50 ≤ 1,00 m koşulunu karşılayanların en ucuzu olarak bunu buldu. Her aday tam bir benzetimdi, yani sonuç tablonun geldiği motordan geliyor. Askı sayısı iki katından fazla; kilometre başına maliyet de onunla artıyor."""
note_en = """Tunnel brackets at 120 m instead of 250 m. A placement search tried arrangements of the tunnel row against the real ground it stands on and found this the cheapest to meet availability ≥ 99,0 % and HPE P50 ≤ 1,00 m. Every candidate was a full simulation, so the result comes from the same engine as the table. It takes more than twice the brackets, and the cost per kilometre rises with them."""

[values]
'tunnel.anchor_spacing_m' = 120.0
