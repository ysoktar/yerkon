# Example result tables

Generated by `python -m locbench3d.cli run-all --config examples/experiment_standard.yaml`,
Monte Carlo seed 0. These are a snapshot from one successful benchmark run,
kept here as an example of the shape and content of a real run's output;
regenerate your own with the command above (outputs to `output/standard/`
by default, not committed to version control).

See `manifest.json` for the row count and columns of each table, and
`../example_workbook.xlsx` for the same run's populated Excel workbook.
